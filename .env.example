# テスト配信（/api/test-delivery）に必要な環境変数。
# このファイルをコピーして .env.local を作り、値を入れる（.env.local は commit されない）。

# Resend の API キー（https://resend.com/api-keys）
RESEND_API_KEY=

# 送信元アドレス。Resend で検証済みのドメインでなければ送信が拒否される
MAIL_FROM=
